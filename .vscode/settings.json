{
    "files.exclude": {
        "**/*.js": { 
            "when": "$(basename).ts"
        },
        "**/*.js.map": { 
            "when": "$(basename)"
        }
    },
    "git.ignoreLimitWarning": true,
    "angular.enable-strict-mode-prompt": false
}